{
  "name": "apps-api",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-remote.mjs",
    "dev:local": "wrangler dev src/index.ts",
    "dev:remote": "npm run dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env preview",
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "@pump/core": "*",
    "@pump/db": "*",
    "@pump/shared": "*",
    "hono": "^4.4.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "wrangler": "^4.21.0"
  }
}
